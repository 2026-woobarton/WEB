# 이 파일을 .env 로 복사한 뒤 실제 키를 채우세요.
#   cp .env.example .env
#
# 주의: Vite 는 VITE_ 로 시작하는 값을 빌드 결과물(JS 번들)에 그대로 포함시킵니다.
# 따라서 이 키는 배포된 사이트에서 누구나 볼 수 있습니다. 개인용/시연용으로만 쓰고,
# 공개 서비스라면 키를 감추는 서버를 두고 그쪽을 호출하세요.
VITE_OPENAI_API_KEY=
